[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: Integration tests with real Keycloak (may be slow)
    manual: Manual tests requiring user interaction
addopts = -v --strict-markers --tb=short

