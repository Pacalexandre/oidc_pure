# =============================================================================
# CONFIGURAÇÃO GENÉRICA OIDC - Multi-Provider
# =============================================================================
# Use estas variáveis para qualquer provedor OIDC (Keycloak, Google, etc.)
# Troque apenas o OIDC_PROVIDER para alternar entre provedores
# =============================================================================

# PROVEDOR ATIVO (keycloak, google, github, microsoft, okta, auth0, custom)
OIDC_PROVIDER=keycloak

# CONFIGURAÇÃO GENÉRICA (usado por todos os provedores)
OIDC_ISSUER_URL=https://seu-provedor.com/auth/realms/SEU_REALM
OIDC_CLIENT_ID=seu-client-id
OIDC_CLIENT_SECRET=seu-client-secret
OIDC_REDIRECT_URI=http://localhost:5400/callback
OIDC_SCOPES=openid profile email

# CONFIGURAÇÕES ADICIONAIS
OIDC_USE_PKCE=true
OIDC_VERIFY_SSL=true
OIDC_TOKEN_LEEWAY=60

# =============================================================================
# PROVEDORES PRÉ-CONFIGURADOS
# Para ativar um provedor, mude OIDC_PROVIDER acima e descomente as linhas
# =============================================================================

# -----------------------------------------------------------------------------
# KEYCLOAK - Provedor padrão
# -----------------------------------------------------------------------------
KEYCLOAK_ISSUER_URL=https://seu-keycloak.com/auth/realms/SEU_REALM
KEYCLOAK_CLIENT_ID=seu-client-id
KEYCLOAK_CLIENT_SECRET=seu-client-secret
KEYCLOAK_REDIRECT_URI=http://localhost:5400/callback
KEYCLOAK_SCOPES=openid profile email

# -----------------------------------------------------------------------------
# GOOGLE OAUTH
# -----------------------------------------------------------------------------
# Obtenha suas credenciais em: https://console.cloud.google.com/apis/credentials
GOOGLE_ISSUER_URL=https://accounts.google.com
GOOGLE_CLIENT_ID=seu-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-seu-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5400/callback
GOOGLE_SCOPES=openid profile email

# -----------------------------------------------------------------------------
# GITHUB OAUTH (GRATUITO) - OAuth2 puro, não OIDC
# -----------------------------------------------------------------------------
# Obtenha suas credenciais em: https://github.com/settings/developers
# 1. Acesse GitHub → Settings → Developer settings → OAuth Apps
# 2. Clique em "New OAuth App"
# 3. Configure Authorization callback URL: http://localhost:5400/callback
# NOTA: GitHub usa OAuth2 puro (não OIDC), endpoints são pré-configurados
GITHUB_ISSUER_URL=https://github.com
GITHUB_CLIENT_ID=seu-client-id
GITHUB_CLIENT_SECRET=seu-client-secret
GITHUB_REDIRECT_URI=http://localhost:5400/callback
GITHUB_SCOPES=user:email read:user

# -----------------------------------------------------------------------------
# MICROSOFT AZURE AD
# -----------------------------------------------------------------------------
# Obtenha suas credenciais em: https://portal.azure.com/
# MICROSOFT_ISSUER_URL=https://login.microsoftonline.com/common/v2.0
# MICROSOFT_CLIENT_ID=seu-application-id
# MICROSOFT_CLIENT_SECRET=seu-client-secret
# MICROSOFT_REDIRECT_URI=http://localhost:5400/callback
# MICROSOFT_SCOPES=openid profile email User.Read

# -----------------------------------------------------------------------------
# OKTA
# -----------------------------------------------------------------------------
# Obtenha suas credenciais no painel do Okta
# OKTA_ISSUER_URL=https://seu-dominio.okta.com/oauth2/default
# OKTA_CLIENT_ID=seu-client-id
# OKTA_CLIENT_SECRET=seu-client-secret
# OKTA_REDIRECT_URI=http://localhost:5400/callback
# OKTA_SCOPES=openid profile email

# -----------------------------------------------------------------------------
# AUTH0
# -----------------------------------------------------------------------------
# Obtenha suas credenciais no painel do Auth0
# AUTH0_ISSUER_URL=https://seu-dominio.auth0.com
# AUTH0_CLIENT_ID=seu-client-id
# AUTH0_CLIENT_SECRET=seu-client-secret
# AUTH0_REDIRECT_URI=http://localhost:5400/callback
# AUTH0_SCOPES=openid profile email

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
APP_HOST=localhost
APP_PORT=5400
APP_SECRET_KEY=gere-uma-chave-secreta-aleatoria-de-64-caracteres-hexadecimais

# =============================================================================
# INSTRUÇÕES
# =============================================================================
#
# 1. Copie este arquivo para .env:
#    $ cp .env.example .env
#
# 2. Configure as credenciais do provedor desejado
#
# 3. Para gerar APP_SECRET_KEY:
#    $ python -c "import secrets; print(secrets.token_hex(32))"
#
# 4. Registre o redirect_uri no console do provedor:
#    - Google: https://console.cloud.google.com/apis/credentials
#    - GitHub: https://github.com/settings/developers
#    - Microsoft: https://portal.azure.com/
#    - Keycloak: Admin Console → Clients → Valid Redirect URIs
#
# 5. IMPORTANTE: Nunca commite o arquivo .env com credenciais reais!
#
# =============================================================================
